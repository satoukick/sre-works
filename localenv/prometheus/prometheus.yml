global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus self-monitoring
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # # OTel Collector internal metrics
  # - job_name: otel-collector
  #   static_configs:
  #     - targets: ["otel-collector:8888"]

  # sre-works app (if a /metrics endpoint is exposed in the future)
  # - job_name: sre-works
  #   static_configs:
  #     - targets: ["app:8080"]
