apiVersion: v1
kind: Service
metadata:
  name: sre-practice-service
spec:
  selector:
    app: sre-practice-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
